<nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4">
    <div class="container">
        <a class="navbar-brand fw-bold" href="/">EventHub</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbar">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbar">
            <ul class="navbar-nav me-auto">
                <li class="nav-item"><a class="nav-link" href="/">Domů</a></li>
                <li class="nav-item"><a class="nav-link" href="/events">Akce</a></li>
                {% if session.user %}
                    <li class="nav-item"><a class="nav-link" href="/event/create">Přidat akci</a></li>
                    <li class="nav-item"><a class="nav-link" href="/my-events">Moje akce</a></li>
                    {% if session.user.role in ['moderator','admin','superadmin'] %}
                        <li class="nav-item"><a class="nav-link" href="/moderator">Moderátor</a></li>
                    {% endif %}
                    {% if session.user.role in ['admin','superadmin'] %}
                        <li class="nav-item"><a class="nav-link" href="/admin">Admin</a></li>
                    {% endif %}
                {% endif %}
            </ul>
            <ul class="navbar-nav">
                {% if session.user %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">
                            {{ session.user.username }} ({{ session.user.role }})
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="/logout">Odhlásit se</a></li>
                        </ul>
                    </li>
                {% else %}
                    <li class="nav-item"><a class="nav-link" href="/login">Přihlásit</a></li>
                    <li class="nav-item"><a class="nav-link" href="/register">Registrovat</a></li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
